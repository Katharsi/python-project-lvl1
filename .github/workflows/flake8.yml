name: flake8

on:
  - push
  - pull_request

jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: linter-test
        run: |
          pip install poetry
          make install
      - name: linter
        run: |
          make lint
